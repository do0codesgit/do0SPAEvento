@model IEnumerable<do0.Models.do0TipoEvento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeEvento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Observacoes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeEvento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Observacoes)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.do0TipoEventoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.do0TipoEventoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.do0TipoEventoId })
        </td>
    </tr>
}

</table>
